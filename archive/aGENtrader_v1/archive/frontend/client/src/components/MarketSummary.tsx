import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function MarketSummary() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Market Summary</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Market summary component placeholder</p>
      </CardContent>
    </Card>
  );
}
