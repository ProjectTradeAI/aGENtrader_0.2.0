import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PositionsList() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Positions List</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Positions list component placeholder</p>
      </CardContent>
    </Card>
  );
}
